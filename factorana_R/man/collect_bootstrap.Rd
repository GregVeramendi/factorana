% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_factorana.R
\name{collect_bootstrap}
\alias{collect_bootstrap}
\title{Collect finished bootstrap samples into standard errors and intervals}
\usage{
collect_bootstrap(stage_dir, conf_level = 0.95, require_convergence = TRUE)
}
\arguments{
\item{stage_dir}{Directory of per-sample result files.}

\item{conf_level}{Confidence level for percentile intervals.}

\item{require_convergence}{Drop replicates with \code{convergence != 0}.}
}
\value{
A \code{factorana_bootstrap} list with \code{boot_se}, \code{boot_cov}, \code{ci}
(a data frame of percentile intervals and bootstrap SEs), the stacked
\code{estimates}, and counts (\code{n_total}, \code{n_converged}, \code{n_used}).
}
\description{
Reads every \verb{sample_<id>.rds} in \code{stage_dir}, stacks the estimates, and
returns the bootstrap covariance, bootstrap standard errors, and percentile
confidence intervals. Non-converged replicates are dropped by default.
}
\seealso{
\code{\link[=generate_bootstrap_samples]{generate_bootstrap_samples()}}, \code{\link[=bootstrap_fit_sample]{bootstrap_fit_sample()}}
}
