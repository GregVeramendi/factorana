% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_factorana.R
\name{generate_bootstrap_samples}
\alias{generate_bootstrap_samples}
\title{Generate and persist bootstrap resampling weights}
\usage{
generate_bootstrap_samples(data, R, cluster = NULL, dir = NULL, seed = NULL)
}
\arguments{
\item{data}{Data frame used for estimation.}

\item{R}{Number of bootstrap replicates.}

\item{cluster}{Optional cluster id: a column name in \code{data} or a vector of
length \code{nrow(data)}. If \code{NULL}, an ordinary row (nonparametric) bootstrap
is used (each row is its own cluster).}

\item{dir}{Optional directory to write the samples object to (as
\code{bootstrap_samples.rds}). Created if it does not exist.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A \code{factorana_boot_samples} list with \code{weights} (an integer
\code{nrow(data)} x \code{R} matrix), and metadata (\code{R}, \code{nobs}, \code{n_clusters}).
Invisibly returns the same object when \code{dir} is supplied.
}
\description{
Draws \code{R} bootstrap replicates by resampling clusters (or rows) with
replacement and records, for each replicate, the integer frequency weight of
every row (the number of times its cluster was drawn). The weights are saved
once so that the same replicate is reproducible across nodes and across
restarts.
}
\seealso{
\code{\link[=bootstrap_fit_sample]{bootstrap_fit_sample()}}, \code{\link[=collect_bootstrap]{collect_bootstrap()}}, \code{\link[=bootstrap_factorana]{bootstrap_factorana()}}
}
