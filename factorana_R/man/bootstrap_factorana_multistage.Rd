% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_factorana.R
\name{bootstrap_factorana_multistage}
\alias{bootstrap_factorana_multistage}
\title{Multi-stage single-node bootstrap driver}
\usage{
bootstrap_factorana_multistage(
  stage_builders,
  data,
  R,
  cluster = NULL,
  dir,
  control = NULL,
  seed = NULL,
  conf_level = 0.95,
  ...
)
}
\arguments{
\item{stage_builders}{A list of functions, one per stage, each with the
signature \verb{function(prev_fits, data)} returning the \code{model_system} for that
stage. For the first stage \code{prev_fits} is an empty list; for later stages
\code{prev_fits[[k]]} is the fitted result of stage \code{k} for the current
replicate (use it to construct the stage's \code{previous_stage}).}

\item{data}{Data frame.}

\item{R}{Number of bootstrap replicates.}

\item{cluster}{Optional cluster id (column name in \code{data} or a vector); see
\code{\link[=generate_bootstrap_samples]{generate_bootstrap_samples()}}.}

\item{dir}{Directory holding the samples object and the per-stage
subdirectories (\verb{stage_1/}, \verb{stage_2/}, ...).}

\item{control}{Estimation control.}

\item{seed}{Optional seed for sample generation.}

\item{conf_level}{Confidence level for percentile intervals.}

\item{...}{Passed to \code{\link[=estimate_model_rcpp]{estimate_model_rcpp()}} for every stage fit.}
}
\value{
A \code{factorana_bootstrap_multistage} list with a per-stage
\code{\link[=collect_bootstrap]{collect_bootstrap()}} summary in \code{stages} and \code{final} pointing at the last
stage.
}
\description{
Runs a multi-stage bootstrap on one machine: for each replicate, fits each
stage in turn, chaining every stage on that replicate's own earlier-stage
fits. Each (stage, sample) fit is written to \verb{dir/stage_<k>/sample_<id>.rds}
and skipped if it already exists, so an interrupted run resumes where it
stopped. If a stage fails to converge for a replicate, its later stages are
skipped (so a bad earlier fit is not chained forward).
}
\details{
For distributed (multi-node) runs, drive the same per-stage, per-sample work
with \code{\link[=bootstrap_fit_sample]{bootstrap_fit_sample()}} directly (one array-job task per sample), which
is what this driver does internally.
}
\seealso{
\code{\link[=bootstrap_factorana]{bootstrap_factorana()}}, \code{\link[=bootstrap_fit_sample]{bootstrap_fit_sample()}}, \code{\link[=collect_bootstrap]{collect_bootstrap()}}
}
