% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vcov_factorana.R
\name{vcov_factorana}
\alias{vcov_factorana}
\title{Robust and cluster-robust covariance for a fitted factorana model}
\usage{
vcov_factorana(
  object,
  data,
  type = c("hessian", "robust", "cluster"),
  cluster = NULL,
  n_quad = NULL,
  finite_sample = TRUE
)
}
\arguments{
\item{object}{A \code{factorana_result} from \code{\link[=estimate_model_rcpp]{estimate_model_rcpp()}}.}

\item{data}{The data frame used to fit \code{object} (same rows, same order).}

\item{type}{Covariance type: \code{"hessian"} (inverse information, the default,
reproduces the fit's standard errors), \code{"robust"} (Huber-White), or
\code{"cluster"} (cluster-robust).}

\item{cluster}{For \code{type = "cluster"}: either the name of a column in \code{data}
holding the cluster id, or a vector of length \code{nrow(data)}.}

\item{n_quad}{Number of quadrature points for recomputing the per-observation
scores. Defaults to the value used at estimation (stored on \code{object}).}

\item{finite_sample}{Logical; apply the usual finite-sample scaling
(\code{n/(n-k)} for robust; \code{G/(G-1) * (N-1)/(N-k)} for cluster). Default \code{TRUE}.}
}
\value{
A \verb{nparam x nparam} covariance matrix over the full parameter vector,
with row/column names equal to the parameter names. Fixed and
equality-tied parameters have zero rows and columns. Standard errors are
\code{sqrt(diag(.))}.
}
\description{
Computes a sandwich (Huber-White) or cluster-robust covariance matrix for a
model fitted with \code{\link[=estimate_model_rcpp]{estimate_model_rcpp()}}, as a post-hoc alternative to the
default inverse-Hessian (model-based) standard errors. The estimator is
\deqn{V = H^{-1} B H^{-1},}
where the "bread" \eqn{H^{-1}} is the inverse observed information already
computed at fit time and the "meat" \eqn{B} is built from the
per-observation scores of the marginal (factor-integrated) log-likelihood:
\eqn{B = \sum_i g_i g_i'} (robust) or \eqn{B = \sum_c s_c s_c'} with
\eqn{s_c = \sum_{i \in c} g_i} (cluster).
}
\details{
One factorana observation is one row of \code{data}, with its measurements
integrated jointly over the latent factors. The latent factor therefore
already models the within-row dependence. Clustering is meaningful only when
a cluster groups several rows (for example several individuals in the same
household or school, or several stacked person-wave rows for one person in a
long-format second stage). With one row per cluster, the cluster estimator
reduces to the ordinary robust estimator.

For two-stage estimates these standard errors treat the first-stage
(measurement) parameters and any plug-in factor scores as known, so they
understate uncertainty (the generated-regressor problem). Use a bootstrap
that re-runs both stages for honest two-stage inference.
}
\seealso{
\code{\link[=estimate_model_rcpp]{estimate_model_rcpp()}}
}
