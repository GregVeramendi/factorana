% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vcov_factorana.R
\name{robust_se}
\alias{robust_se}
\title{Robust / cluster-robust standard errors for a fitted factorana model}
\usage{
robust_se(
  object,
  data,
  type = c("robust", "cluster"),
  cluster = NULL,
  n_quad = NULL,
  finite_sample = TRUE
)
}
\arguments{
\item{object}{A \code{factorana_result} from \code{\link[=estimate_model_rcpp]{estimate_model_rcpp()}}.}

\item{data}{The data frame used to fit \code{object} (same rows, same order).}

\item{type}{Covariance type: \code{"hessian"} (inverse information, the default,
reproduces the fit's standard errors), \code{"robust"} (Huber-White), or
\code{"cluster"} (cluster-robust).}

\item{cluster}{For \code{type = "cluster"}: either the name of a column in \code{data}
holding the cluster id, or a vector of length \code{nrow(data)}.}

\item{n_quad}{Number of quadrature points for recomputing the per-observation
scores. Defaults to the value used at estimation (stored on \code{object}).}

\item{finite_sample}{Logical; apply the usual finite-sample scaling
(\code{n/(n-k)} for robust; \code{G/(G-1) * (N-1)/(N-k)} for cluster). Default \code{TRUE}.}
}
\value{
Named numeric vector of standard errors (fixed/tied parameters are 0).
}
\description{
Convenience wrapper returning \code{sqrt(diag(vcov_factorana(...)))} as a named
vector over the full parameter vector. See \code{\link[=vcov_factorana]{vcov_factorana()}} for details
and caveats.
}
\seealso{
\code{\link[=vcov_factorana]{vcov_factorana()}}
}
