% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_factorana.R
\name{bootstrap_fit_sample}
\alias{bootstrap_fit_sample}
\title{Estimate one stage for one bootstrap sample (restartable)}
\usage{
bootstrap_fit_sample(
  model_system,
  data,
  samples,
  sample_id,
  stage_dir,
  control = NULL,
  ...,
  weight_col = ".bootw",
  overwrite = FALSE
)
}
\arguments{
\item{model_system}{A \code{model_system} (already carrying \code{previous_stage} for
this sample if estimating a later stage).}

\item{data}{The full data frame used to generate the samples.}

\item{samples}{A \code{factorana_boot_samples} object or a path to a
\code{bootstrap_samples.rds} file.}

\item{sample_id}{Integer replicate index (1..R).}

\item{stage_dir}{Directory for this stage's per-sample result files.}

\item{control}{Estimation control (see \code{\link[=define_estimation_control]{define_estimation_control()}}).}

\item{...}{Passed to \code{\link[=estimate_model_rcpp]{estimate_model_rcpp()}} (e.g. \code{optimizer}, \code{parallel}).}

\item{weight_col}{Name of the temporary weight column added to the data.}

\item{overwrite}{If \code{TRUE}, re-estimate even if the output file exists.}
}
\value{
Invisibly, the path to the written result file.
}
\description{
Fits \code{model_system} to the data reweighted by bootstrap sample \code{sample_id}
and writes the result to \verb{stage_dir/sample_<id>.rds}. If that file already
exists it returns immediately, so an interrupted run (or a rebooted node)
simply re-runs the missing samples. This is the unit of work to scatter
across a compute cluster (one array-job task per \code{sample_id}).
}
\details{
Rows whose bootstrap weight is zero are dropped, and the remaining rows are
given their integer frequency weight via \code{model_system$weights}. For a
multi-stage workflow, set \code{model_system$previous_stage} (built from this
sample's previous-stage fit) before calling, so each replicate chains on its
own earlier stage.
}
\seealso{
\code{\link[=generate_bootstrap_samples]{generate_bootstrap_samples()}}, \code{\link[=collect_bootstrap]{collect_bootstrap()}}
}
