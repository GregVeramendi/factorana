% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_factor_model.R
\name{simulate_factor_model}
\alias{simulate_factor_model}
\title{Simulate data from a factorana model}
\usage{
simulate_factor_model(
  object,
  data,
  n = NULL,
  params = NULL,
  weights = NULL,
  seed = NULL,
  detail = TRUE
)
}
\arguments{
\item{object}{A \code{factorana_result} from \code{\link[=estimate_model_rcpp]{estimate_model_rcpp()}}, or a
\code{model_system}. If a \code{model_system}, supply \code{params}.}

\item{data}{Data frame whose rows are resampled to supply the covariates.}

\item{n}{Number of units to simulate (default: \code{nrow(data)}).}

\item{params}{Named parameter vector. Defaults to \code{object$estimates} when
\code{object} is a fitted result.}

\item{weights}{Optional resampling weights: a column name in \code{data} or a
numeric vector of length \code{nrow(data)}.}

\item{seed}{Optional integer seed.}

\item{detail}{If \code{TRUE} (default), include per-component linear predictor
(\code{Vobs}), factor contributions (\verb{Vfac<k>}), shock (\code{eps}), and choice
probabilities, like the legacy detailed output.}
}
\value{
A data frame with one row per simulated unit: an \code{xid}, the resampled
covariates, the drawn latent factor(s) (\verb{factor_<k>}), and per component the
simulated outcome (named after the component) plus, when \code{detail = TRUE},
the diagnostic columns.
}
\description{
Generates a simulated data set from a fitted or fully specified factorana
model, following the legacy simulation algorithm: for each simulated unit a
base row is drawn from \code{data} (with weights) to supply the covariates, the
latent factor(s) are drawn from the model's unconditional distribution, and
each component's outcome is drawn from its model (linear, probit, logit, or
ordered probit) given the covariates and factors.
}
\details{
Supports all four model types (linear, probit, logit including multinomial,
ordered probit), latent types, mixtures of normals, and the independent,
correlated, and structural-equation (\verb{SE_*}) factor structures. The simulated
outcome column is named after each component's outcome variable, so the
returned data frame can be re-estimated with the same model system.
}
\seealso{
\code{\link[=estimate_model_rcpp]{estimate_model_rcpp()}}
}
