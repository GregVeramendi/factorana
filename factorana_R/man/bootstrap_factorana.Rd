% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_factorana.R
\name{bootstrap_factorana}
\alias{bootstrap_factorana}
\title{Single-node bootstrap driver (convenience over the primitives)}
\usage{
bootstrap_factorana(
  model_system,
  data,
  R,
  cluster = NULL,
  stage_dir,
  control = NULL,
  seed = NULL,
  conf_level = 0.95,
  ...
)
}
\arguments{
\item{model_system}{A \code{model_system}.}

\item{data}{Data frame used for estimation.}

\item{R}{Number of bootstrap replicates.}

\item{cluster}{Optional cluster id: a column name in \code{data} or a vector of
length \code{nrow(data)}. If \code{NULL}, an ordinary row (nonparametric) bootstrap
is used (each row is its own cluster).}

\item{stage_dir}{Directory for per-sample result files (and the samples object).}

\item{control}{Estimation control.}

\item{seed}{Optional integer seed for reproducibility.}

\item{conf_level}{Confidence level for percentile intervals.}

\item{...}{Passed to \code{\link[=estimate_model_rcpp]{estimate_model_rcpp()}}.}
}
\value{
A \code{factorana_bootstrap} object (see \code{\link[=collect_bootstrap]{collect_bootstrap()}}).
}
\description{
Generates \code{R} bootstrap samples, fits every replicate in a local loop (each
fit is restartable via \verb{stage_dir/sample_<id>.rds}), and collects the
results. For a single estimation stage on one machine. For multi-stage,
distributed, or restart-heavy workflows, use the primitives
(\code{\link[=generate_bootstrap_samples]{generate_bootstrap_samples()}}, \code{\link[=bootstrap_fit_sample]{bootstrap_fit_sample()}},
\code{\link[=collect_bootstrap]{collect_bootstrap()}}) directly so each stage and sample is an independent,
resumable job.
}
\seealso{
\code{\link[=generate_bootstrap_samples]{generate_bootstrap_samples()}}, \code{\link[=bootstrap_fit_sample]{bootstrap_fit_sample()}}, \code{\link[=collect_bootstrap]{collect_bootstrap()}}
}
